export const environment = {
    production: false,
    /**
     * Esta es la URL para desarrollo local.
     * Apunta a tu contenedor de Docker (FastAPI) que corre en el puerto 8000.
     */
    apiUrl: 'http://localhost:8000'
};